{
  "name": "apollo-agenda",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Apollo agenda body (DEV · APOLLO_AGENDA_REACT) — React inside a transparent WKWebView in place of the native timeline and month grid.",
  "scripts": {
    "dev": "vite --port 5320",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "~5.9.3",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
